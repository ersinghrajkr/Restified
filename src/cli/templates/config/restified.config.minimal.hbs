/**
 * RestifiedTS Configuration - Getting Started
 * 
 * This is a minimal configuration to get you started quickly.
 * See CONFIGURATION.md for advanced features.
 */

import { RestifiedConfig } from 'restifiedts';

const config: RestifiedConfig = {
  // 🚀 Basic API Configuration
  clients: {
    api: {
      baseURL: '{{baseUrl}}'
    }
  }
};

export default config;